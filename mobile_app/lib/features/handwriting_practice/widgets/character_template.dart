// Character template display widget
import 'package:flutter/material.dart';

class CharacterTemplate extends StatelessWidget {
  final String character;

  const CharacterTemplate({super.key, required this.character});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement character template display
    return Container();
  }
}
